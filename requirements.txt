# ── Core app ──────────────────────────────────────────────────────────────────
streamlit>=1.32.0,<2.0.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.18.0

# ── ML runtime  (scikit-learn version MUST match the version used to train
#    the .pkl models — bump carefully and retrain if upgrading) ───────────────
scikit-learn>=1.3.0,<1.6.0
joblib>=1.3.0

# ── Export ────────────────────────────────────────────────────────────────────
openpyxl>=3.1.0
reportlab>=4.0.0

# ── Notebook-only dependencies (not required to run app.py) ──────────────────
xgboost>=2.0.0
shap>=0.44.0
prophet>=1.1.5
